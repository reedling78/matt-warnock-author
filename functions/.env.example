# Copy to functions/.env and fill in. Non-secret config only.
# Secrets (SMTP_USER, SMTP_PASS) are set separately with:
#   firebase functions:secrets:set SMTP_USER
#   firebase functions:secrets:set SMTP_PASS

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
CONTACT_TO=matt@mattwarnockauthor.com
CONTACT_FROM=Matt Warnock Site <no-reply@mattwarnockauthor.com>
